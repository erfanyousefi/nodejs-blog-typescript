import 'reflect-metadata';
import { Application, RequestHandler } from 'express';
import { Controller, RouterLib } from './decorators/types';
export declare class Server {
    private readonly _app;
    private _showLogs;
    private static readonly LOG_STR;
    constructor(showLogs?: boolean);
    get app(): Application;
    get showLogs(): boolean;
    set showLogs(showLogs: boolean);
    addControllers(controllers: Controller | Controller[], routerLib?: RouterLib, globalMiddleware?: RequestHandler): void;
    private wrapErrorMiddleware;
    private getRouter;
}
